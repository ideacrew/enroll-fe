import { ResidenceMonthsPipe } from './residence-months.pipe';

describe('ResidenceMonthsPipe', () => {
  it('create an instance', () => {
    const pipe = new ResidenceMonthsPipe();
    expect(pipe).toBeTruthy();
  });
});
